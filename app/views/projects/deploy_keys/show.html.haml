%h3.page-title
  部署密钥:
  = @key.title
  %small
    创建于
    = @key.created_at.strftime("%Y年%m月%d日")
.back-link
  = link_to project_deploy_keys_path(@project) do
    &larr; 将密钥列表

%hr
%pre= @key.key
.pull-right
  = link_to '删除', project_deploy_key_path(@project, @key), data: { confirm: '你确定吗?'}, method: :delete, class: "btn-remove btn delete-key"
