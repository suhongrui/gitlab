%h3.page-title
  代码段
  - if can? current_user, :write_project_snippet, @project
    = link_to new_project_snippet_path(@project), class: "btn btn-new pull-right", title: "新的代码段" do
      添加新的代码段

%p.light
  与他人分享git库粘贴出来的代码

%hr
%ul.bordered-list
  = render partial: "projects/snippets/snippet", collection: @snippets
  - if @snippets.empty?
    %li
      .nothing-here-block 这里没有什么。
