!!! 5
%html{ lang: "zh-CN"}
  = render "layouts/head"
  %body.ui_mars.login-page.application
    = render "layouts/broadcast"
    = render "layouts/public_head_panel", title: ''
    .container.navless-container
      .content
        = render "layouts/flash"
        .row.prepend-top-20
          .col-sm-5.pull-right
            = yield
          .col-sm-7.brand-holder.pull-left
            %h1
              = brand_title
            - if brand_item
              = brand_image
              = brand_text
            - else
              %h3 开源软件的代码协作

              %p
                管理Git仓库，使您的代码的安全细粒度的访问控制。
                %br
                执行代码审查，并加强与合并请求合作。
                %br
                每一个项目也可以有一个问题跟踪器和wiki。

            - if extra_sign_in_text.present?
              = markdown(extra_sign_in_text)

    %hr
    .container
      .footer-links
        = link_to "浏览", explore_root_path
        = link_to "文档", "http://www.gituse.com/"
        = link_to "关于GitUse", "https://www.gituse.com/"
