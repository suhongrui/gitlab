.event-title
  %span.author_name= link_to_author event
  %span.event_label{class: event.action_name}
    = event_action_name(event)

  - if event.project
    = link_to_project event.project
  - else
    = event.project_name

- if current_user == event.author && !event.project.private? && twitter_sharing_enabled?
  .event-body
    .event-note
      .md
        %p
          恭喜！为什么不与世界分享你的成就？

          %a.twitter-share-button{ |
            href: "https://twitter.com/share", |
            "data-url" => event.project.web_url, |
            "data-text" => "我刚刚创建一个新的GitUse项目！ GitUse是你的服务器上的版本控制。", |
            "data-size" => "medium", |
            "data-related" => "gitlab", |
            "data-hashtags" => "gitlab", |
            "data-count" => "none"}
            分享Tweet
          %script{src: "//platform.twitter.com/widgets.js"}