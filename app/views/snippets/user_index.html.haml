%h3.page-title
  = image_tag avatar_icon(@user.email), class: "avatar s24"
  = @user.name
  %span
    \/
    代码段
  - if current_user
    = link_to new_snippet_path, class: "btn btn-small add_new pull-right", title: "新的代码段" do
      添加新的代码段

%hr

= render 'snippets'
